---
trigger: always_on
---

HEADERS AND FOOTERS:
Do not add a footer to any page unless specifically requested. The header on all pages should contain the centred logo of the club (logo_normal). The cog for settings should be on the left. The login and logout button should be on the right.

NAVIGATION BAR:
The Navigation bar should be always visible, rest on the far left of the user's window and move with the user's throughout pages. It should float above the rest of the page and be centred in the left middle. The navigation bar should have 6 main categories represented by icons that individually expand when hovered. The main categories are: About, Events, Connect, Members, Communications, Resources. The categories should then open up a menu to the right for subcategories. The main category selection should be a toggle and be able to untoggled to hide the subpages.